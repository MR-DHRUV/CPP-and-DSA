#include <bits/stdc++.h>
using namespace std;



int main()
{
  
    // expectation : avg value of a random variable 
    // a random variable is outcome of an experiment like throwing a coin, rolling a dice
    
    // random variables can have 
    // 1 Discrete values
    // 2 Continous values

    // say a random var x represents temprature so x can be 10.1 10.2 10 ... for days
    // say a random var y represents the outcome of rolling a dice, so y can only take values from 1 to 6, so its discrete as outcomes have discrete values 

    // for discrete
    // E(x) = sum of (xi * P(xi))

    // E[x] is expectation of x 
    // p(x) is probability function 


    // for continous .. not used much in programming
    // E[x] = integration (x.P(x) dx)

    // linearity of expectation   
    // E[x1 + x2] = E[x1] + E[x2]

    return 0;
}