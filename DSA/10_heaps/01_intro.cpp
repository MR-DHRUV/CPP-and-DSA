#include<bits/stdc++.h>
using namespace std;



int main()
{
    // heap is a complete binary tree that comes with a heap order property.
    // complete binary tree : every level is completely filled except the last level

    // mostly represented in an array
    // height = logN

    // it is of 2 types
    // max heap : every node should have element greater than or equal to its decendants
    // min heap : every node should have element less than or equal to its decendants

    // duplicates are allowed
    
    // For a node at index i 
    // its left child will be at : 2i
    // its right child will be at : 2i + 1
    // parent will be at : i/2 

    return 0;
}
