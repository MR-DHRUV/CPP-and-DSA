#include<bits/stdc++.h>
using namespace std;



int main()
{
    // heap is a complete binary tree mostly represented in an array
    // height = logN

    // min heap : every node should have element greater than or equal to its decendants
    // max heap : every node should have element less than or equal to its decendants

    // duplicates are allowed
    
    // For a node at index i 
    // its left child will be at : 2i
    // its right child will be at : 2i + 1

    return 0;
}