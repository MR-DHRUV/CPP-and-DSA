#include<bits/stdc++.h>
using namespace std;



int main()
{
    // widely used data structure due to lesser time complexity and wide applications
    // we can do insertion, deletion and searching in  O(1) time complexity

    // unordered map -> hash table : O(1)
    // map -> bst(red black trees) : O(logN)

    return 0;
}