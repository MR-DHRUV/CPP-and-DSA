#include<bits/stdc++.h>
using namespace std;



int main()
{
    // Avl tree : height balanced bst also called perfect bst
    // pages in google/ yaaho etc are stored in form of avl trees
    // height is balanced to make searching fast    

    // we calc balace factor to make a bst height balanced
    // balance factor : h(left) - h(right)

    // valid bf are -1 , 0 , 1
    // that is for a bst to be height balanced the diffrence between heights of right subtree and left subtree should not ne more than 1;
 
    // to balance a bst we perform rotations

    // Ratations at the time of insertion of nodes
    // there 4 types of rotations
    // LL Rotation
    // RR Rotation
    // LR Rotation
    // RL Rotation

    
    

    return 0;
}